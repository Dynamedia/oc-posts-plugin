# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    slug:
        label: dynamedia.posts::lang.common.labels.slug
